// import { renderWithProvider } from 'src/test/utils'
// import { Header } from './Header'

describe('Header', () => {
  it('matches snapshot', () => {
    // const { getByText } = renderWithProvider(<Header />)
    // const address = getByText(NULL_ADDRESS)
    // expect(address).toBeTruthy()
  })
})
